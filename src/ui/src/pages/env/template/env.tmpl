<div>
  <div b:show="{evnName}">
    <div b:show="{filePath}">
      <div><b>Editing file:</b> {filePath}</div>
      <!--{reasons}-->
    </div>
    <div b:hide="{filePath}">No files is editing in the host</div>
  </div>
  <div b:hide="{evnName}">no env detected</div>
</div>
